<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test_lab_week_12">

    <!-- Izin Internet Wajib untuk akses API TMDB -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MovieApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TEST_LAB_WEEK_12"
        tools:targetApi="31">

        <!-- Activity Utama (Halaman Awal) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Detail Movie -->
        <!-- Pastikan nama class di sini sesuai dengan file Kotlin yang kamu buat.
             Jika kamu membuatnya dengan nama 'MovieDetailActivity', ubah namanya di bawah ini.
             Jika namanya 'DetailsActivity', biarkan seperti ini. -->
        <activity
            android:name=".MovieDetailActivity"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>
